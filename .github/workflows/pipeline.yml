name: CI/CD_GCP_Actions

on:
  workflow_dispatch:
  push:
    branches:
    - main

jobs: 
  terraform:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: terraform

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Terraform Init
        id: init
        run: terraform init